import React, { FC } from 'react'
import { TimersContainer } from '../containers/TimersContainer.styled'

const Timers: FC = () => {
   return <TimersContainer></TimersContainer>
}

export default Timers
